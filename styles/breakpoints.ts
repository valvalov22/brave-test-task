const size = {
  xs: "319px", // for small screen mobile
  xm: "326px", // for mobile screen
  md: "708px", // for tablets
  tb: "800px",
};

export const device = {
  xs: `(max-width: ${size.xs})`,
  xm: `(max-width: ${size.xm})`,
  md: `(max-width: ${size.md})`,
  tb: `(max-width: ${size.tb})`,
};
